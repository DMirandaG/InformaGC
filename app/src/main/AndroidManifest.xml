<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pem.informagc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".vista.VistaLogin"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".vista.VistaRecuperarPassword"
            android:theme="@style/AppTheme.Transparent"
            android:label=""
            android:parentActivityName=".vista.VistaLogin" />
        <activity android:name=".vista.VistaRegistro"
            android:theme="@style/AppTheme.Transparent"
            android:label=""
            android:parentActivityName=".vista.VistaLogin" />
        <activity android:name=".vista.VistaPasoUno"
            android:label="Paso 1 de 2"
            android:theme="@style/AppTheme.Transparent2"
            />
        <activity android:name=".vista.VistaPasoDos"
            android:label="Paso 2 de 2"
            android:theme="@style/AppTheme.Transparent2"
            android:parentActivityName=".vista.VistaPasoUno"
            >

        </activity>
    </application>

</manifest>